# Task Brief — super-agent-code

## Request
- Skill scope anchor: `super-agent-code` is the cross-domain orchestrator.
- Objective:
- Requested deliverable(s):
- Deadline (absolute date, YYYY-MM-DD):

## Routing decision
- Chosen sub-skill: `zra-master-ops` | `zra-special-ops` | `deep-coded`
- Signals matched (cite ROUTING.md row(s)):
- Alternatives considered:
- Why rejected:
- Tie-breaker applied (if any):
- Multi-phase plan? (yes/no — if yes, list phases and the sub-skill for each):

## Sub-skill scaffold pointer
- Sub-skill scaffold to copy alongside this brief:
  - `zra-master-ops` → `.agents/skills/zra-master-ops/scaffold/`
  - `zra-special-ops` → `.agents/skills/zra-special-ops/scaffold/`
  - `deep-coded` → `.agents/skills/deep-coded/scaffold/`
- Active sub-skill brief filename in this run folder:
- Active sub-skill validation-log filename in this run folder:

## Inputs and sources
- Source file(s):
- Relevant modules / packages / exhibits / CSVs:
- External reference(s) (issues, PRs, filings, docs):

## Traceability split
- Facts:
- User statements:
- Assumptions:
- Inferences:

## Constraints
- Skill preservation check: confirm `super-agent-code` ran the routing pass and the chosen sub-skill is now the active skill.
- Non-destructive requirements:
- Security / secrets constraints:
- Scope boundaries (what is explicitly out of scope):

## Rollback plan
- How to revert if the change is wrong:
- Re-route trigger: signals that would force a re-route mid-task:
- Observability / signal to watch after merge or delivery:
